#namespace :migrations do

  #desc ""
  #task :move_items_to_lists => :environment do
   # emails = []
    #Items.all do |item|
     # list = List.where(name: item.tag)
      ## list = List.new
        #list.name = item.tag
      #end

      #list.items << item unelss list.items.contains item
      #list.save
    #end
 # end

#end