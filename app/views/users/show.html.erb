<div class="row">
  <div class="col-md-12">
    <h2 class="text-center">Hello! <%= current_user.name || current_user.email %></h2>
  </div>
</div>
<div class="row">
  <div class="col-md-3">
  </div>
  <div class="col-md-6">
    <h3 class="text-center">Your Lists:</h3>
    <ul class="nav nav-pills nav-stacked">
      <% @lists.each do |list| %>
        <li><%=link_to list.name , list_path(current_user, list), class: 'btn btn-default btn-lg btn-block' %></li>
      <% end %>
    </ul>
    <%= link_to "New List", new_list_path(current_user), class: 'btn btn-primary btn-lg btn-block'%>
  </div>
  <div class="col-md-3">
  </div>
</div>